%h2
  TODO list
%ul
  - @todos.each do |todo|
    %li
      = todo.title
      %form{action: finished_todo_path(todo), method: :post}
        %button{type: :submit}
          Finish
= link_to 'New', new_todo_path